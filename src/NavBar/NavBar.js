import React from 'react';
import './NavBar.css';

const NavBar = (props) => {
    return (
        <nav>
            <div className={'max-width--700'}>
                <p>Phrasebook</p>

            </div>
        </nav>
    );
};

export default NavBar